<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Fase 25 — visibilidade de pacotes necessária para OPEN_APP e OPEN_URL
         (nunca QUERY_ALL_PACKAGES; allowlist restrita por <queries>). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <!-- Fase 27.2 (F13): usesCleartextTraffic="true" libera cleartext para a LAN
         do Core (http://192.168.x.x:8100); o WAN sempre usa TLS (wss/https). Um
         NetworkSecurityConfig com domains não resolveria: a LAN é um IP arbitrário
         do usuário, não listável como domain. A defesa em profundidade fica no
         cliente: WanEndpoint.validate só aceita wss:// em produção e http(s)
         explícito na LAN. -->
    <application
        android:name=".VegaApplication"
        android:label="VEGA"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:theme="@style/Theme.Vega"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>